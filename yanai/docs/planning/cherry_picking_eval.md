# Execution Plan: Cherry-Picking Evaluation (Topic B)

## 1. Ideation & Hypotheses
**Research Question:** Does bidirectional entanglement hold consistently across an exhaustive and diverse array of entities, or were initial examples strategically cherry-picked? Specifically, does subliminally prompting a generated number reliably increase the likelihood of its entangled subject?
**Hypothesis:** Subliminal bidirectional mapping represents an artifact of systemic LLM optimization topology mapping conceptual dimensions over arbitrary strings. Therefore, the entanglement phenomenon should remain structurally robust universally regardless of obscurity across a diverse animal distribution. 
**Variables Setup:** 
- The Subject Domain: At least 40 distinct animals varying from highly common (cats, dogs, lions) to significantly obscure (pangolins, axolotls, quokkas).
- Evaluated metrics: Baseline Entity Probability vs Prompted Probability mapped via their specific entangled numeric string.

## 2. Phase 2: Implementation 
- **Code Structuring (DRY):**
  - Rigidly utilize the utility infrastructure provided inside `topic_b_utils` combining `run_experiment` handling extraction algorithms mapping exactly:
    1. Base Animal Probability determination.
    2. Model-associated Number derivation constraint sequences. 
    3. Re-evaluated Probability extraction evaluating Subliminal Injection formats natively.
- **Experiment Script (`experiments/cherry_picking_eval.py`):**
  - Build extensive lists defining over 40 distinct valid strings representing the animal targets.
  - Sequentially batch executions via a localized tracking loop, passing the `category="animal"` abstraction into the baseline evaluating `run_experiment(model, tokenizer, animal, category)` natively.
  - Dump results (animal, num_target, baseline, subliminal, improvement_ratio) onto explicit formats generating caching capabilities saving data correctly to `experiments/cache/cherry_picking_eval.json`.

## 3. Phase 3: Checkpointing & Tracking
- **Scribe Notebook:**
  - Create `notebooks/cherry_picking_scribe.ipynb` mapping standard histogram arrays and tabular analytics plotting exactly how massive variance distributions affect ratios proving absolute universal presence or selective isolation empirically natively using standard Matplotlib tracking arrays securely against the compiled dictionary matrices.

## 4. Phase 4: Full Execution
- Execute script iterating across the heavy inference mapping structure seamlessly deploying inference calculations linearly over the GPU infrastructure evaluating language probability outputs.

## 5. Phase 5: Post-Experiment Evaluation
- Establish explicit ideological observations outlining comprehensive proofs responding to arbitrary conceptual entanglement bounds mathematically into final documentation mapping inside standard `reports` domains.
