# Implementation Plan: Clean Rerun of GSNR Experiment

## Overview
My previous changes to the experiment code (`dropout_analysis_sweep.py`) to track weight-change means were mathematically invalid because the 10-model ensemble uses independent initializations. Although I reverted the code and updated the plotting script, the data currently sitting in `outputs/dropout_15e_stage.json` was generated by the *invalid* code version. To guarantee the scientific integrity and reproducibility of the workspace, we must re-run the experiment using the clean, reverted code.

## 1. Current State Verification
- **`dropout_analysis_sweep.py`**: Successfully reverted. It now tracks only valid metrics: weight-change variance (`Layer2_Weight_Change_Var_Ghost`) and the Teacher's signal magnitude (`Teacher_Ghost_Logit_Mag`).
- **`plot_weight_change_var.py`**: Properly configured to parse the valid metrics and calculate GSNR without the negative artifact bounds.

## 2. Re-Running the Experiment
- Launch the SLURM job using `sbatch revised_scripts/dropout_analysis_sweep.slurm`.
- This will cleanly overwrite `outputs/dropout_15e_stage.json` with the mathematically valid dataset.

## 3. Re-Generating Plots
- Once the SLURM job completes, I will re-run `scratch/rebuild_dropout_analysis.py` and `scratch/plot_weight_change_var.py` to recreate the PDFs and PNGs cleanly from the new JSON.

## Next Steps
Upon your explicit approval of this plan, I will dispatch the SLURM job, monitor its completion, re-generate the plots, and confirm the final state.
